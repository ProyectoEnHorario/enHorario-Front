# Carpeta home/data/models

Define estructuras de datos para transporte o persistencia.

## Que va aqui
- Modelos serializables.
- Metodos `fromMap`, `toMap`, `fromJson`, `toJson`.

## Estructura de nombres
- `nombre_model.dart`.
- Clase principal `NombreModel`.

## Tipos de archivo
- `.dart`.
